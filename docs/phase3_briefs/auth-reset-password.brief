Add a /reset-password endpoint to the auth service. The endpoint should accept an email, generate a single-use token, email the token to the user, and let the user POST back the token plus a new password to complete the reset. Specify the storage location for the token, its lifetime, and how the existing session/refresh-token state should be affected when the reset succeeds.
